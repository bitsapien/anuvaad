// Name : Sherlock
#include <stdio.h>
int main() {
	// input:
	// city_map holds the representation of the city as a table
	int n, m, crime_row_number, crime_col_number;
	 city_map[100][100];
	 idx, jdx;
	scanf("%d", &n);
	scanf("%d %d", &n, &m);
	scanf("", );


	// write your code here

	// output
	// store your results in `crime_row_number`<Integer>, `crime_col_number`<Integer>
	printf("%d %d", crime_row_number, crime_col_number);
	return 0;
}