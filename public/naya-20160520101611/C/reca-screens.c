// Name : Reca Screens
#include <stdio.h>
int main() {
	// input:
	// [a,b] : size in centimeters, and [x,y] : ratio
	int a, b, x, y, resized_a, resized_b;
	scanf("%d %d %d %d", &a, &b, &x, &y);


	// write your code here
	// store result in `resized_a` and `resized_b`

	// output
	printf("%d %d", &resized_a, &resized_b);
	return 0;
}